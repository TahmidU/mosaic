import { cleanup, fireEvent, render, screen } from "utils/test-config";

import Input from ".";
import React from "react";

afterEach(cleanup);

describe("Input", () => {
  test("Render Prefix and Postfix nodes", () => {});

  test("Input text stateless", () => {});

  test("Value prop stateful", () => {});
});
