import { ReactElement } from "react";

interface IDropdownComponent {}

export default function DropdownComponent({}: IDropdownComponent): ReactElement {
  return (
    <div>
      <div></div>
    </div>
  );
}
