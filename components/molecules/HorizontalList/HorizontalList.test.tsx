import { cleanup, fireEvent, render, screen } from "utils/test-config";

afterEach(cleanup);

describe("HorizontalList", () => {
  test("Title props", () => {});

  test("Child appearance", () => {});

  test("Loading elements appearance", () => {});

  test("Next button click", () => {});

  test("Previous button click", () => {});
});
